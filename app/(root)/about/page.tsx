import { Metadata, NextPage } from 'next'

export const metadata: Metadata = {
    title: "WOLF1 - About",
    description: "about WOLF",
}

interface Props { }

const Page: NextPage<Props> = ({ }) => {
    return <main className="flex flex-col items-center h-screen">
       
    </main>
}

export default Page